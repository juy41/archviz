# docs/

Drop the animated demo here as `demo.gif` — the root `README.md` references it
as `![demo](docs/demo.gif)`.

Suggested recording: load the **Microservices** template, edit a couple of
lines so viewers see the diagram update live, toggle the theme, then click
**Copy link**. Keep it under ~10 seconds and around 1000px wide.
